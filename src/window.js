// import dropDown from './components/DropDown/dropDown.module.scss';
// import select from './components/Select/select.module.scss';

// const windowEvents = () => {
//   window.onclick = ({ target }) => {
//     if (target && target.classList) {
//       if (
//         !target.classList.value.includes('select')
//       ) {
//         removeHandle(select);
//       }
//       if (!target.classList.value.includes('dropDown')) {
//         removeHandle(dropDown);
//       }
//     }
//   };
// };

// const removeHandle = (styleFile) => {
//   document
//     .getElementsByClassName(styleFile.block)[0]
//     ?.classList.remove(styleFile['block-active']);
// };
// export default windowEvents;
